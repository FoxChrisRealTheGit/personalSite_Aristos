<%- include("../_layouts/adminDynamicHeader") %>
    <h2 class="admin-page__main-title">Edit - Dynamic Page Builder</h2>
    <div>
        <button class="btn btn-primary">
            <a class="btn-text" href="/admin/pages">Back to pages</a>
            <button class="btn btn-primary">
                <a class="btn-text" href="/admin/pages/edit-page/<%= id %>">Basic Editor</a>
            </button>
        </button>
    </div>

    <%- include("../../../AristosPageEditor/AristosPageEditor") %>

    <form id="goodFormName" method="post" action="/admin/pages/edit-page/<%= id %>">

        <div class="form-group">
            <label for="">Title</label>
            <input type="text" class="form-control" name="title" value="<%= title %>" placeholder="Title">

        </div>

        <div class="form-group">
            <label for="">Slug</label>
            <input type="text" class="form-control" name="slug" value="<%= slug %>" placeholder="Slug">

        </div>
        <div class="form-group">
            <label for="">Parent</label>
            <select name="parent" class="form-control">
                <% pages.forEach(function(page){ %>
                    <option value="<%= page.slug %>">
                        <%= page.title %>
                    </option>
                    <% }); %>
            </select>
        </div>

            <button id="mostAmazingButton" class="btn-submit btn-text">Submit</button>
    </form>

    <%- include("../_layouts/adminDynamicFooter") %>