<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= description %>">
    <meta name="keywords" content="<%= keywords %>">
    <meta name="author" content="<%= author %>">

    <title>Christopher Fox -
        <%= title %>
    </title>
    <link rel="stylesheet" href="/styles/theme.css">
    <script src='https://www.google.com/recaptcha/api.js'></script>
</head>

<body>
    <% media.forEach(function(medias){ %>
    <% if(medias.category == "site-logo"){ %>
        <div class="header__brand">
            <a class="navbar-brand" href="/"><img src="/<%= medias.path %>" alt="<%= medias.alt %>"></a>
        </div>
            <% } %>
<% }) %>
    <nav class="header__main">
        <div>
            <ul class="header__main">
                <% pages.forEach(function(page){ %>
                    <% if (page.slug == "home"){ %>
                        <li>
                            <a class="header__main--link" href="/">
                                <%= page.title %>
                            </a>
                        </li>
                        <% } else{ %>
                            <li>
                                <a class="header__main--link" href="/<%= page.slug %>">
                                    <%= page.title %>
                                </a>
                            </li>
                            <% } %>
                                <% }) %>

            </ul>
        </div>
    </nav>

    <main role="main" class="container">